---
name: engineering-quality
description: 在前端、后端、全栈、数据、模型、库、基础设施与部署项目中，指导工程实施并支持已有项目的质量审查、整改、验证和交付。
---

# 通用工程化实施与质量审查

## 定位

本 Skill 不是只读扫描器，而是一套贯穿“理解需求、设计、编码、测试、交付、运维和复盘”的工程化工作方法。它不以某个框架或架构为唯一答案，而是根据项目画像、风险、规模和生命周期，帮助 Agent 做出可维护、可验证、可部署、可观测、可恢复的变更。

适用范围包括前端、后端、全栈、命令行工具、软件库、数据管道、机器学习与模型服务、基础设施代码、容器和持续交付工程。没有证据证明需要时，不机械要求微服务、容器、数据库、缓存、消息队列、复杂框架、端到端测试或百分之百覆盖率。

## 工作模式

根据用户意图选择模式；需要改代码时，默认进入实施模式，而不是先做一份与实施脱节的 Review 报告。

### 实施模式

用户要求开发、新增功能、修复缺陷、重构、升级依赖、配置部署或优化时使用：

1. 编码前读取仓库级指令、现有变更、入口、构建文件、测试和部署配置，建立轻量项目画像。
2. 读取通用基线和与项目类型、风险、生命周期有关的规则；只加载当前任务需要的参考文件。
3. 先形成实施计划：需求边界、模块边界、文件变更、数据和接口影响、兼容性、安全、测试、观测、发布与回滚。
4. 实施过程中遵循现有约定，保持单一职责和依赖方向；发现文件或目录规模失控时及时拆分，不以增加文件数量为目的。
5. 新增或修改核心行为时同步补充失败分支、边界条件、权限、超时、日志指标、文档和迁移/回滚处理（按适用性取舍）。
6. 实施后运行与项目阶段匹配的质量门禁，并进行独立复审；复审有问题时先返工再交付。

### 审查模式

用户要求检查、评估、Review、体检或判断是否合格时使用：

- 默认只读，不修改代码、不提交、不推送。
- 先建立项目画像，再按证据逐项审查，输出通过、部分通过、不通过、待确认、未验证和不适用。
- 问题必须包含位置、证据、风险、最小整改建议、验收标准和是否阻断发布。
- 不能把“未发现”“未验证”“没有实现”和“不适用”混为一谈。

### 整改与交付模式

用户明确要求修复、完成修改、提交或推送时使用：

- 在实施模式基础上处理已识别问题，按互不冲突的范围拆分任务并记录工作区原有变更。
- 修改、提交和推送分别视为独立授权；只有用户明确要求对应动作时才执行。
- 完成测试、独立复审、返工和发布门禁后再提交或推送，并报告提交号、远端、分支、验证结果和剩余风险。

## 安全与范围边界

- 不访问生产数据、不调用真实外部服务、不泄露密钥，不执行破坏性操作；必要的联调、迁移或发布须有明确授权和回滚方案。
- 不回滚用户或其他 Agent 已有的工作区修改；先记录 `git status`、差异和当前分支。
- 信息不足时标记“待确认”或“未验证”，不能用猜测替代业务或部署决策。
- 若仓库存在 `AGENTS.md`、贡献指南、构建说明或目录级规则，先读取并遵循；冲突时以更具体、更新且有权限的规则为准。

## 通用执行流程

1. **识别任务和风险**：判断是实施、审查还是整改交付，确定是否涉及数据、权限、外部依赖、生产风险和兼容性。
2. **建立项目画像**：使用 [项目画像模板](references/templates/project-profile-template.md)，记录语言、框架、运行方式、项目类型、生命周期、依赖、数据、暴露面、风险和未知项。
3. **选择规则**：始终读取 [通用基线](references/baseline/universal.md)，再按需读取 `references/project-types/`、`references/workflows/` 和 `references/templates/` 中相关文件；明确启用、不适用和未验证的规则。
4. **设计并实施**：实施模式读取 [实施工作流](references/workflows/implementation.md)；整改交付额外读取 [整改工作流](references/workflows/remediation.md)。使用 [实施计划模板](references/templates/implementation-plan-template.md)。
5. **独立复审**：复杂、跨模块、高风险或用户要求多代理时，按 `subagents-manager` 拆分架构、代码测试、安全、交付运维等互不重复的审查角色；审查者不得擅自修改产物。
6. **质量门禁**：根据项目类型运行编译/语法、格式、静态检查、单元/集成/接口/端到端、构建、依赖和容器检查；无法运行的项目如实记录原因。
7. **交付输出**：实施任务参考 [变更总结模板](references/templates/change-summary-template.md)；审查任务参考 [审查报告模板](references/templates/review-report-template.md)；发布前参考 [发布门禁模板](references/templates/release-gate-template.md)。

## 工程结构规范

对业务仓库和本 Skill 自身都执行以下原则：

- 模块按职责、变化原因和依赖方向拆分；接口、业务、持久化、展示、基础设施和测试不应无边界混杂。
- 大文件、上帝类、过大组件、超长方法、重复流程和高冲突文件优先拆分。建议以单一职责、复杂度、测试难度和变更频率判断；不要为了凑文件数机械拆分。
- 单个目录建议保持约 8–12 个直接子文件以内；超出时按领域、生命周期或资源类型分组。该数值是维护信号，不是绝对硬门禁。
- `SKILL.md` 只放定位、路由、模式、核心流程和安全边界；详细规则、流程、模板和示例放入 `references/`，按需加载。
- 单个参考文件建议只覆盖一个主题，约 250–400 行以内；超过后按通用规则、项目类型、生命周期、模板或示例拆分。
- 任何被流程明确引用的模板必须真实存在且可直接填写，不得只创建标题占位文件；新增领域应同时补规则索引和适用性说明。
- 目录和文件命名保持稳定、可检索、使用小写字母、数字和连字符；避免同义文件、重复规则和失效引用。

## 规则索引

- 通用：`references/baseline/universal.md`
- 项目类型：
  - `references/project-types/frontend.md`
  - `references/project-types/backend.md`
  - `references/project-types/data-and-model.md`
  - `references/project-types/delivery-and-operations.md`
  - `references/project-types/library-and-cli.md`
  - `references/project-types/infrastructure.md`
- 生命周期：`references/workflows/implementation.md`、`review.md`、`remediation.md`
- 模板：`references/templates/project-profile-template.md`、`implementation-plan-template.md`、`review-report-template.md`、`remediation-plan-template.md`、`release-gate-template.md`、`change-summary-template.md`
- Skill 维护：`references/skill-maintenance.md`
- 后续增强：`references/roadmap.md`

## 默认输出顺序

### 实施或整改任务

1. 目标与项目画像；2. 适用规则；3. 实施计划与变更范围；4. 变更摘要；5. 验证命令和结果；6. 复审结论；7. 发布、回滚和剩余风险。

### 审查任务

1. 项目画像和审查范围；2. 启用与不适用规则；3. 总体结论和能力评价；4. 分级问题清单；5. 待确认与未验证；6. 验证记录；7. 整改计划。

详细检查项按需读取对应参考文件，不要在所有项目中机械套用与当前任务无关的规则。