# 审查前向测试场景

请使用 `$engineering-quality` 审查一个已有的前后端项目：前端有一个过大的监控页面组件，后端接口缺少超时和权限校验，数据库有迁移脚本但没有恢复演练，持续集成只执行构建。要求只读审查，不修改代码、不提交、不推送。

## 观察点

- 是否明确选择审查模式并保持只读。
- 是否区分代码证据、执行证据、待确认和未验证。
- 是否输出规则适用性、分级问题、验收标准、发布门禁和整改计划。
- 是否同时覆盖模块拆分、前后端、数据、交付、Git 和恢复，而非只关注代码风格。