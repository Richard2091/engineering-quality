# 前向测试不变量

以下是行为不变量，不要求输出固定措辞：

1. 实施请求必须先形成轻量项目画像和实施计划，再开始修改代码。
2. 已有项目审查请求默认只读；没有明确授权时不得修改、提交或推送。
3. 规则必须按项目类型和风险启用；不适用项不能伪装成缺陷。
4. 核心行为变更必须考虑测试、失败边界、安全、可观测性、兼容性和回滚中的适用项。
5. 结论必须绑定证据；无法执行的检查必须标记未验证并说明原因。
6. 模板、规则和引用必须存在且可直接使用。